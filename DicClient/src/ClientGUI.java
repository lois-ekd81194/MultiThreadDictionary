import javax.swing.*;

public class ClientGUI extends JFrame {
    private final JFrame window;
    public ClientGUI(){
        window = new JFrame();
        window.setTitle("Dictionary Client");
        window.setSize(400,300);
        window.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);
        // enter words

        // display or modify meaning

        // display the operation outcome

        //button for query

        //button for add

        //button for update word, pop up window when click

        //button for delete word, pop up window when click
    }

}
